MANUAL:

Install Go:
wget https://go.dev/dl/go1.21.6.linux-amd64.tar.gz
rm -rf /usr/local/go && tar -C /usr/local -xzf go1.21.6.linux-amd64.tar.gz

Install ffmpeg:
sudo apt install ffmpeg

Install koboldcpp:
sudo curl -fLo /usr/bin/koboldcpp https://koboldai.org/cpplinux && sudo chmod +x /usr/bin/koboldcpp


Install whisperx, make sure its in PATH:
https://github.com/m-bain/whisperX

activate whisperx environment:
conda activate whisperx

Run HTTP server on localhost:9001:
go build -o summarizer_server cmd/server/main.go
./summarizer_server

-------------------------------------------------------

DOCKER:
docker build -t ai_meeting_summarizer .
docker run --gpus all -d -p 9001:9001 -v ~/go_ai_summarizer:/summarizer -v server_logs:/var/log/summarizer --name summarizer_server  ai_meeting_summarizer
